(function() {
	window.DOMHelper = {
		defineComponent: function(mixin) {
			//define an object that will include mixed in methods and properties from the framework's builtin objects
			var obj = {};
			
			if(mixin && mixin.length) {
				
			}
		}
	};
})();